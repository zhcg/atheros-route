TARGET_PREFIX	:= xt-
TARGET_LDFLAGS	:= -g -nostdlib
CROSS_COMPILE	:= $(XTENSA_TOOLS_ROOT)/bin
CC		:= $(CROSS_COMPILE)/$(TARGET_PREFIX)xcc
AS		:= $(CROSS_COMPILE)/$(TARGET_PREFIX)xcc
AR		:= $(CROSS_COMPILE)/$(TARGET_PREFIX)ar
LD		:= $(CROSS_COMPILE)/$(TARGET_PREFIX)xcc
NM		:= $(CROSS_COMPILE)/$(TARGET_PREFIX)nm
OBJCOPY		:= $(CROSS_COMPILE)/$(TARGET_PREFIX)objcopy
OBJDUMP		:= $(CROSS_COMPILE)/$(TARGET_PREFIX)objdump
RM		:= rm
